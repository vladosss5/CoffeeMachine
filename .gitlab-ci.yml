stages:
  - build
  - test

variables:
  test: "CoffeeMachine.IntegrationTest"

build:
  stage: build
  script:
    - docker compose down
    - docker compose build
    - docker compose up --abort-on-container-exit
  artifacts: 
    paths:
      - /built-output